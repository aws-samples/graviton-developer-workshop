{
  "mcpServers": {
    "arm-mcp": {
      "command": "sh",
      "args": [
        "-c",
        "docker run --rm -i -v \"$(pwd)\":/workspace armlimited/arm-mcp:latest"
      ],
      "timeout": 180000
    }
  }
}